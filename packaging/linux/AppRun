#!/bin/sh
# AppImage entry point. Bevy resolves assets from BEVY_ASSET_ROOT, so point it
# at the bundled directory that contains `assets/` (next to the binary).
HERE="$(dirname "$(readlink -f "$0")")"
export BEVY_ASSET_ROOT="$HERE/usr/bin"
exec "$HERE/usr/bin/taro-app" "$@"
